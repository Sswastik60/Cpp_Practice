#include <stdio.h>

int main(){
    int a;
    char b;
    float c;
    double d;
    long int e;
    long double f;
    printf("int= %d,char = %d,float= %d,double =%d,long int =%d,long Double =%d",sizeof(a),sizeof(b),sizeof(c),sizeof(d),sizeof(e),sizeof(f));
    return 0;
}